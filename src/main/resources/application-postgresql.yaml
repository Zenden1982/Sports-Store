spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/sports_store
        username: postgres
        password: 123
    jpa:
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
        hibernate:
            ddl-auto: update
